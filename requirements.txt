fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pandas>=2.2
numpy>=1.26
scikit-learn>=1.5
lightgbm>=4.4
aiofiles>=24.1
jinja2>=3.1
cloudpickle>=3.0
# Notebook-side deps (the server doesn't need these but the same
# requirements.txt is what students install for the workshop).
requests>=2.31
matplotlib>=3.8
seaborn>=0.13
